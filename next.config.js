/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.dhamma.org'], // Add any other allowed domains here
  }
  
}

module.exports = nextConfig
